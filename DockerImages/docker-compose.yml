services:

  keycloak:
    build: ./KeyClock
    container_name: keycloak
    command: start-dev --import-realm -Dkeycloak.theme.default=apiengine
    environment:
      KEYCLOAK_ADMIN: admin
      KEYCLOAK_ADMIN_PASSWORD: admin
      KC_DB: mysql
      KC_DB_URL: jdbc:mysql://mysql:3306/keycloak
      KC_DB_USERNAME: keycloak
      KC_DB_PASSWORD: keycloak
      KC_HOSTNAME: localhost
      KC_HOSTNAME_PORT: 8081
      KC_HOSTNAME_STRICT: false
      KC_HTTP_ENABLED: true
      KC_SMTP_HOST: smtp.gmail.com
      KC_SMTP_PORT: 587
      KC_SMTP_FROM: "API engine email verification <fadi.freij38601@gmail.com>"
      KC_SMTP_USERNAME: fadi.freij38601@gmail.com
      KC_SMTP_PASSWORD: eolpmkyblwesfjax
      KC_SMTP_STARTTLS: true
    ports:
      - "8081:8080"
    depends_on:
      mysql:
        condition: service_healthy
    volumes:
      - ./KeyClock/realm:/opt/keycloak/data/import
      - ./KeyClock/theme:/opt/keycloak/themes

  mysql:
    build: ./mysql
    container_name: mysql
    restart: always
    environment:
      MYSQL_DATABASE: keycloak
      MYSQL_USER: keycloak
      MYSQL_PASSWORD: keycloak
      MYSQL_ROOT_PASSWORD: root
    ports:
      - "3307:3306"
    volumes:
      - mysql_data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      timeout: 5s
      retries: 10

  sqlserver:
    build: ./sqlserver
    container_name: sqlserver
    environment:
      ACCEPT_EULA: "Y"
      SA_PASSWORD: "Welcome@123"
    ports:
      - "1433:1433"
    volumes:
      - sqlserver_data:/var/opt/mssql
    healthcheck:
      test: ["CMD-SHELL", "/opt/mssql-tools18/bin/sqlcmd -S localhost -U sa -P 'Welcome@123' -Q 'SELECT 1' || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 10

  # dotnet-app:
  #   build: ./back-end-app
  #   container_name: dotnet-app
  #   depends_on:
  #     sqlserver:
  #       condition: service_healthy
  #     keycloak:
  #       condition: service_healthy
  #   environment:
  #     ASPNETCORE_ENVIRONMENT: Development
  #     ASPNETCORE_URLS: "http://+:8080"
  #     ConnectionStrings__DefaultConnection: "Server=sqlserver;Database=MyDb;User Id=sa;Password=Welcome@123;"
  #     Keycloak__Authority: "http://keycloak:8080"
  #   ports:
  #     - "5000:8080"

volumes:
  mysql_data:
  sqlserver_data:
